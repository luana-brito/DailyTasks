# Opcional. Na Vercel com deploy unificado (api/index.mjs + Turso), NÃO defina — o front usa /api no mesmo site.
# Use só se a API estiver em outro domínio (sem barra no final).
# VITE_API_URL=https://sua-api.example.com

# SMS (opcional) — ver README
# VITE_SMS_API_KEY=
# VITE_SMS_API_URL=
